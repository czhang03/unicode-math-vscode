{
	"name": "unicode-math-input",
	"displayName": "Unicode Math Input",
	"description": "Easy way to insert unicode math symbols ∀ programming languages",
	"version": "0.5.1",
	"publisher": "czhang03",
	"license": "LGPL-3.0",
	"homepage": "https://github.com/czhang03/unicode-math-vscode",
	"icon": "icon.png",
	"repository": {
		"type": "git",
		"url": "https://github.com/czhang03/unicode-math-vscode"
	},
	"engines": {
		"vscode": "^1.74.0"
	},
	"categories": [
		"Snippets"
	],
	"keywords": [
		"unicode",
		"math",
		"symbols",
		"shortcuts",
		"snippets"
	],
	"activationEvents": [
		"onStartupFinished"
	],
	"main": "./out/main.js",
	"contributes": {
		"commands": [
			{
				"command": "unicode-math-input.commit",
				"title": "commit the string before the cursor to unicode",
				"category": "Unicode Math"
			}
		],
		"keybindings": [
			{
				"command": "unicode-math-input.commit",
				"key": "tab",
				"when": "editorTextFocus && !inlineSuggestionVisible && !suggestWidgetVisible && !editorTabMovesFocus && !inSnippetMode && !hasSnippetCompletions && !editorTabMovesFocus && !editorReadonly"
			}
		],
		"configuration": {
			"title": "Unicode Math Input",
			"properties": {
				"unicodeMath.TriggerStrings": {
					"title": "Trigger Strings (deprecated)",
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"\\"
					],
					"description": "This is the strings that triggers the completion and commit, default is '\\'",
					"markdownDeprecationMessage": "This setting will be deprecated at 1.0, please use `unicodeMathInput.TriggerStrings` instead"
				},
				"unicodeMathInput.TriggerStrings": {
					"title": "Trigger Strings",
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"\\"
					],
					"description": "This is the strings that triggers the completion and commit, default is '\\'"
				},
				"unicodeMath.WarnPrefixFail": {
					"type": "string",
					"enum": ["on", "off", "concise"],
					"enumDescriptions": [
						"send a warning when some character cannot be converted to the font described by the prefix",
						"do not send a warning when some character cannot be converted to the font described by the prefix",
						"send a concise (modal) warning when some character cannot be converted to the font described by the prefix"
					],
					"default": [
						"on"
					],
					"description": "Send a warning when there is some character cannot be converted to unicode in the prefix conversion"
				},
				"unicodeMath.WarnSymbolFail": {
					"type": "string",
					"enum": ["on", "off", "concise"],
					"enumDescriptions": [
						"send a warning when some LaTeX command cannot be converted  to unicode",
						"do not send a warning when some LaTeX command cannot be converted to unicode",
						"send a concise (modal) warning when some LaTeX command cannot be converted to unicode"
					],
					"default": [
						"off"
					],
					"description": "Send a warning when a symbol cannot be converted to unicode"
				},
				"unicodeMathInput.SubscriptPrefixes": {
					"title": "Subscript Prefixes",
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"_", "sub"
					],
					"description": "The prefixes to trigger conversion to unicode subscripts, this string do not include the trigger string."
				},
				"unicodeMathInput.SuperscriptPrefixes": {
					"title": "Superscript Prefixes",
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"^", "sup"
					],
					"description": "The prefixes to trigger conversion to unicode superscripts, this string do not include the trigger string."
				},
				"unicodeMathInput.ItalicPrefixes": {
					"title": "Italic font Prefixes",
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"it", "it:", "mit", "mit:", "mathit", "mathit"
					],
					"description": "The prefixes to trigger conversion to italic font unicode, this string do not include the trigger string."
				},
				"unicodeMathInput.BoldPrefixes": {
					"title": "Bold font Prefixes",
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"bf", "bf:", "mbf", "mbf:", "mathbf", "mathbf:"
					],
					"description": "The prefixes to trigger conversion to bold font unicode, this string do not include the trigger string."
				},
				"unicodeMathInput.MathCalPrefixes": {
					"title": "mathcal font Prefixes",
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"cal", "cal:", "mcal", "mcal:", "mathcal", "mathcal:"
					],
					"markdownDescription": "The prefixes to trigger conversion to `\\mathcal{}` font unicode, this string do not include the trigger string."
				},
				"unicodeMathInput.MathFrakPrefixes": {
					"title": "mathfrak font Prefixes",
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"frak", "frak:", "mfrak", "mfrak:", "mathfrak", "mathfrak:"
					],
					"markdownDescription": "The prefixes to trigger conversion to `\\mathfrak{}` font unicode, this string do not include the trigger string."
				},
				"unicodeMathInput.MathBBPrefixes": {
					"title": "mathbb font Prefixes",
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"bb", "bb:", "mbb", "mbb:", "mathbb", "mathbb:"
					],
					"markdownDescription": "The prefixes to trigger conversion to `\\mathbb{}` font unicode, this string do not include the trigger string."
				},
				"unicodeMathInput.mathsfPrefixes": {
					"title": "mathsf font Prefixes",
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"sf", "sf:", "msf", "msf:", "mathsf", "mathsf:"
					],
					"markdownDescription": "The prefixes to trigger conversion to `\\mathsf{}` font unicode, this string do not include the trigger string."
				},
				"unicodeMathInput.mathttPrefixes": {
					"title": "mathtt font Prefixes",
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"tt", "tt:", "mtt", "mtt:", "mathtt", "mathtt:"
					],
					"markdownDescription": "The prefixes to trigger conversion to `\\mathtt{}` font unicode, this string do not include the trigger string."
				},
				"unicodeMathInput.mathscrPrefixes": {
					"title": "mathscr font Prefixes",
					"type": "array",
					"items": {
						"type": "string"
					},
					"default": [
						"scr", "scr:", "mscr", "mscr:", "mathscr", "mathscr:"
					],
					"markdownDescription": "The prefixes to trigger conversion to `\\mathscr{}` font unicode, this string do not include the trigger string."
				}
			}
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run esbuild-base -- --minify",
		"esbuild-base": "esbuild ./src/extension.ts --bundle --outfile=out/main.js --external:vscode --format=cjs --platform=node",
		"esbuild": "npm run esbuild-base -- --sourcemap",
		"esbuild-watch": "npm run esbuild-base -- --sourcemap --watch",
		"test-compile": "tsc -p ./",
		"lint": "eslint \"src/**/*.ts\"",
		"pretest": "npm run test-compile",
		"test": "node ./out/tests/runTest.js"
	},
	"capabilities": {
		"completionProvider": {
			"resolveProvider": true
		}
	},
	"devDependencies": {
		"@types/glob": "^8.1.0",
		"@types/mocha": "^10.0.1",
		"@types/node": "^18.14.6",
		"@types/vscode": "^1.76.0",
		"@typescript-eslint/eslint-plugin": "^5.54.1",
		"@typescript-eslint/parser": "^5.54.0",
		"@vscode/test-electron": "^2.3.0",
		"@vscode/vsce": "^2.18.0",
		"esbuild": "^0.17.12",
		"eslint": "^8.35.0",
		"eslint-config-prettier": "^8.6.0",
		"eslint-plugin-jsdoc": "^40.0.1",
		"fast-check": "^3.7.0",
		"mocha": "^10.2.0",
		"purify-ts": "^1.3.5",
		"typescript": "^4.9.5",
		"ovsx": "^0.8.0"
	}
}
